(define (log message)
  (display message)
  (newline)
)

(define (f return)
  (log 2)
  (return 3)
  ("💥")
)

(define (main args)
  (define count 0)
  (log 1)
  (log (call/cc (lambda (return)
    (set! saved-return return)
    (f return)
  )))
  (log 4)
  (if (< count 3) (begin
    (set! count (+ 1 count))
    (log "🚀")
    (saved-return count)
  ))
)
