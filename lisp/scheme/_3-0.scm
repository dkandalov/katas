(define (log message)
  (display message)
  (newline)
)

(define (f)
  (call/cc (lambda (return)
    (set! saved-return return)
    (log 2)
    (log 3)
  ))
)

(define (main args)
  (define count 0)
  (log 1)
  (f)
  (log 4)
  (if (< count 3) (begin
    (set! count (+ 1 count))
    (log "🚀")
    (saved-return count)
  ))
)
